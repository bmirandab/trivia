<%= form_with(model: user_game, local: true) do |form| %>
  <% if user_game.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_game.errors.count, "error") %> prohibited this user_game from being saved:</h2>

      <ul>
        <% user_game.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :users_id %>
    <%= form.text_field :users_id %>
  </div>

  <div class="field">
    <%= form.label :games_id %>
    <%= form.text_field :games_id %>
  </div>

  <div class="field">
    <%= form.label :color %>
    <%= form.text_field :color %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
